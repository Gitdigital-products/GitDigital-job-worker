# GitDigital Job Worker
Executes queued tasks (model runs, evaluations, training jobs).  
Coordinates with sandbox + GPU scheduler.

## Features
- Celery / Redis queue
- Retries + fail logging
- Execution metrics
